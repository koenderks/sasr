set.seed(120495)

options(width = 77)

library(jfa)
library(ggplot2)
library(downlit)
library(xml2)

knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>",
  cache = TRUE,
  fig.show = "hold",
  fig.align = "center",
  fig.width = 5,
  fig.height = 4,
  fig.pos = "H",
  out.width = NULL
)
